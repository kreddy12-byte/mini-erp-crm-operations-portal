PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Future phases — required once authentication and Prisma are introduced.
# Do not commit real values. Generate a long random JWT secret later.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mini_erp_crm
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=1d
