import { ModulePlaceholder } from './ModulePlaceholder.tsx';

export function ChallansPage() {
  return (
    <ModulePlaceholder
      title="Sales Challans"
      description="Delivery challans will live here once sales operations are enabled."
      emptyTitle="No sales challans yet"
      emptyDescription="Create a challan after products and customers are available in a later phase."
    />
  );
}
