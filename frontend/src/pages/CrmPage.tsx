import { ModulePlaceholder } from './ModulePlaceholder.tsx';

export function CrmPage() {
  return (
    <ModulePlaceholder
      title="CRM / Follow-ups"
      description="Follow-up work will live here once the CRM pipeline is enabled."
      emptyTitle="No follow-ups yet"
      emptyDescription="Customer follow-ups will appear here once the CRM pipeline is enabled."
    />
  );
}
